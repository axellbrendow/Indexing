{
    "tasks": [
        {
            "type": "shell",
            "label": "Deletar arquivo árvore",
            "command": "rm",
            "args": [
                "TesteArvore.txt"
            ]
        },
        {
            "type": "shell",
            "label": "g++.exe build active file",
            "command": "C:/cygwin64/bin/g++.exe",
            "args": [
                "-g",
                "${file}",
                "-o",
                "${fileDirname}\\${fileBasenameNoExtension}.exe"
            ],
            "options": {
                "cwd": "C:/cygwin64/bin"
            }
        }
    ],
    "version": "2.0.0"
}